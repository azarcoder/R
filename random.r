#The runif() function generates random numbers between 0 and 1 by default

#10 random numbers between -50 too 50

#for one random number
# rnum <- runif(1, -50, 50)

#for 10 random number
rnum <- runif(10, -50, 10)
print(rnum)
