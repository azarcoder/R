v1 <- c("apple", "orange", "melon", "kiwi")
v2 <- c("tomato", "banana", "peri", "grapes")

#syntax should be like this otherwise if else doesn't work
if ("banana1" %in% v1 || "banana1" %in% v2){
    print(TRUE)
    }else{
    print("not")
}
